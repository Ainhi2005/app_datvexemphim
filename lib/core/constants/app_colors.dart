import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromARGB(255, 0, 0, 0);
  static const Color secondary = Color(0xFFFFD700);
  static const Color background = Color.fromARGB(255, 0, 0, 0);
  static const Color surface = Color.fromARGB(255, 0, 0, 0);
  static const Color cardColor = Color(0xFF16213E);
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8D99AE);
  static const Color rating = Color(0xFFFFD700);
  static const Color gradientStart = Color(0xFFE94560);
  static const Color gradientEnd = Color(0xFF533483);
  static const Color error = Color.fromARGB(
    255,
    209,
    0,
    0,
  ); // Cho nút đăng xuất, Snackbar báo lỗi
  static const Color success = Color(0xFF4CAF50); // Cho Snackbar thành công
  static const Color textButton = Color(
    0xFF000000,
  ); // Cho chữ màu đen trong nút Vàng
}
