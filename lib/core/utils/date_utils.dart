/// Tiện ích xử lý ngày tháng toàn app.
/// Ví dụ: format ngày, so sánh, tính tuổi,...
class AppDateUtils {
  static String formatDate(String dateString) {
    // Format date logic here
    return dateString;
  }
}